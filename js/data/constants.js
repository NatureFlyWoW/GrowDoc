// GrowDoc Companion — Shared constants
//
// Single source of truth for values that were previously duplicated
// across multiple views (onboarding, my-grow, plant-detail, etc.).

/** Standard pot-size options (liters) used by every pot-size selector. */
export const POT_SIZES = [1, 3, 5, 7, 10, 15, 20];

/** Log entry types recognized throughout the app. */
export const LOG_TYPES = [
  'water',
  'feed',
  'train',
  'observe',
  'decision',
  'diagnosis',
];
